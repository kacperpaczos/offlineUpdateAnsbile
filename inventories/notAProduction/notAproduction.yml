[all:vars]
ansible_connection=ssh
ansible_user=user

[testServers]
1.2.3.4

[testServers:vars]
ansible_ssh_common_args='-o ProxyCommand="ssh -R 4444:localhost:8080 -W %h:%p -q {{ ansible_user }}@{{ testServers }}"'